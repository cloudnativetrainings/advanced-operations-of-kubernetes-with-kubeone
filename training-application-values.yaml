name: my-app
version: v1
color: lightgrey
message: "Hello from the app installed via helm into k8s cluster created via k1"
catMode: true
persistMetaInfo: false

deployment:
  replicas: 3

service:
  type: ClusterIP

ingress:
  enabled: true
  domain: hubert-04.k1.del-codespaces.cloud-native.training
